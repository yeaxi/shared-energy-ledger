# Test-time dependencies. Kept minimal and pinned to a compatible range for
# reproducible local runs and CI.

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-homeassistant-custom-component>=0.13.180
freezegun>=1.5.0
